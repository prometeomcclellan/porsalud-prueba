import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tg.hn.porsalud.camara',
  appName: 'camara porsalud',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
